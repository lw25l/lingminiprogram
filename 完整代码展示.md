# 灵山胜境AI数字人智能导览系统 - 完整代码文件

## 📋 文件清单（46个文件）

### 一、小程序全局配置（3个文件）

#### 1. app.json
```json
{
  "pages": [
    "pages/index/index",
    "pages/chat/chat",
    "pages/map/map",
    "pages/route/route",
    "pages/attraction/attraction",
    "pages/profile/profile"
  ],
  "window": {
    "backgroundTextStyle": "light",
    "navigationBarBackgroundColor": "#4A90D9",
    "navigationBarTitleText": "灵山胜境",
    "navigationBarTextStyle": "white"
  },
  "tabBar": {
    "color": "#999999",
    "selectedColor": "#4A90D9",
    "backgroundColor": "#ffffff",
    "borderStyle": "black",
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "首页",
        "iconPath": "assets/icons/home.png",
        "selectedIconPath": "assets/icons/home-active.png"
      },
      {
        "pagePath": "pages/chat/chat",
        "text": "智能导游",
        "iconPath": "assets/icons/chat.png",
        "selectedIconPath": "assets/icons/chat-active.png"
      },
      {
        "pagePath": "pages/map/map",
        "text": "导览地图",
        "iconPath": "assets/icons/map.png",
        "selectedIconPath": "assets/icons/map-active.png"
      },
      {
        "pagePath": "pages/profile/profile",
        "text": "我的",
        "iconPath": "assets/icons/profile.png",
        "selectedIconPath": "assets/icons/profile-active.png"
      }
    ]
  },
  "sitemapLocation": "sitemap.json",
  "permission": {
    "scope.userLocation": {
      "desc": "您的位置信息将用于为您提供导航服务"
    }
  },
  "requiredPrivateInfos": ["getLocation", "chooseLocation"]
}
```

#### 2. app.js
```javascript
// app.js
App({
  globalData: {
    userInfo: null,
    userId: null,
    apiBaseUrl: 'https://api.lingshan-guide.com/api',
    selectedInterests: [],
    chatHistory: [],
    visitedAttractions: []
  },

  onLaunch() {
    this.initUser();
    this.checkUpdate();
  },

  initUser() {
    const userId = wx.getStorageSync('userId') || this.generateUserId();
    const userInfo = wx.getStorageSync('userInfo');
    const selectedInterests = wx.getStorageSync('selectedInterests') || [];
    const visitedAttractions = wx.getStorageSync('visitedAttractions') || [];

    this.globalData.userId = userId;
    this.globalData.userInfo = userInfo;
    this.globalData.selectedInterests = selectedInterests;
    this.globalData.visitedAttractions = visitedAttractions;

    wx.setStorageSync('userId', userId);
  },

  generateUserId() {
    return 'user_' + Date.now() + '_' + Math.random().toString(36).substr(2, 9);
  },

  checkUpdate() {
    if (wx.canIUse('getUpdateManager')) {
      const updateManager = wx.getUpdateManager();
      updateManager.onCheckForUpdate((res) => {
        if (res.hasUpdate) {
          updateManager.onUpdateReady(() => {
            wx.showModal({
              title: '更新提示',
              content: '新版本已经准备好，是否重启应用？',
              success: (res) => {
                if (res.confirm) {
                  updateManager.applyUpdate();
                }
              }
            });
          });
        }
      });
    }
  },

  saveInterests(interests) {
    this.globalData.selectedInterests = interests;
    wx.setStorageSync('selectedInterests', interests);
  },

  saveVisitedAttraction(attractionId) {
    const visited = this.globalData.visitedAttractions;
    if (!visited.includes(attractionId)) {
      visited.push(attractionId);
      this.globalData.visitedAttractions = visited;
      wx.setStorageSync('visitedAttractions', visited);
    }
  },

  addChatHistory(message) {
    this.globalData.chatHistory.push(message);
    if (this.globalData.chatHistory.length > 100) {
      this.globalData.chatHistory = this.globalData.chatHistory.slice(-100);
    }
  },

  clearChatHistory() {
    this.globalData.chatHistory = [];
    wx.removeStorageSync('chatHistory');
  }
});
```

#### 3. app.wxss
```css
/* app.wxss - 全局样式 */
page {
  --primary-color: #4A90D9;
  --secondary-color: #6BB3F0;
  --accent-color: #FFB84D;
  --success-color: #52C41A;
  --warning-color: #FAAD14;
  --error-color: #F5222D;
  
  --text-primary: #333333;
  --text-secondary: #666666;
  --text-light: #999999;
  --text-white: #FFFFFF;
  
  --bg-primary: #F5F7FA;
  --bg-secondary: #FFFFFF;
  --bg-card: #FFFFFF;
  
  --border-color: #E8E8E8;
  --shadow-light: 0 2rpx 12rpx rgba(0, 0, 0, 0.08);
  --shadow-medium: 0 4rpx 20rpx rgba(0, 0, 0, 0.12);
  
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
  font-size: 28rpx;
  color: var(--text-primary);
  background-color: var(--bg-primary);
}

/* 通用按钮样式 */
.btn-primary {
  background: var(--primary-color);
  color: var(--text-white);
  border-radius: 50rpx;
  padding: 20rpx 40rpx;
  font-size: 32rpx;
  font-weight: 500;
  border: none;
  text-align: center;
}

.btn-secondary {
  background: var(--bg-secondary);
  color: var(--primary-color);
  border: 2rpx solid var(--primary-color);
  border-radius: 50rpx;
  padding: 18rpx 38rpx;
  font-size: 30rpx;
}

/* 卡片样式 */
.card {
  background: var(--bg-card);
  border-radius: 16rpx;
  box-shadow: var(--shadow-light);
  padding: 24rpx;
  margin-bottom: 20rpx;
}

/* 标签样式 */
.tag {
  display: inline-block;
  padding: 8rpx 20rpx;
  border-radius: 20rpx;
  font-size: 24rpx;
  margin-right: 12rpx;
  margin-bottom: 12rpx;
}

.tag-primary {
  background: rgba(74, 144, 217, 0.1);
  color: var(--primary-color);
}

.tag-success {
  background: rgba(82, 196, 26, 0.1);
  color: var(--success-color);
}

/* 分割线 */
.divider {
  height: 1rpx;
  background: var(--border-color);
  margin: 20rpx 0;
}

/* 加载状态 */
.loading {
  text-align: center;
  padding: 40rpx;
  color: var(--text-light);
}
```

---

## 说明

由于文件较多（46个），我已展示了最核心的3个全局配置文件。

**如果您需要查看其他文件，请告诉我：**

1. **首页相关** - index.wxml, index.wxss, index.js
2. **聊天页相关** - chat.wxml, chat.wxss, chat.js
3. **地图页相关** - map.wxml, map.wxss, map.js
4. **路线页相关** - route.wxml, route.wxss, route.js
5. **景点详情页** - attraction.wxml, attraction.wxss, attraction.js
6. **个人中心页** - profile.wxml, profile.wxss, profile.js
7. **工具函数** - utils/api.js, utils/knowledge.js
8. **后端代码** - server/app.js, server/api/*.js

请回复您想查看的文件编号或名称！
